APP_BASE_URL=http://localhost:3000
CRON_SECRET=replace-with-a-long-random-secret
EMAIL_FROM=66EMP Alerts <digest@alerts.thecaseygroup.us>
EMAIL_TO=ddonofrio@thecaseygroup.us,jason@thecaseygroup.us
RESEND_API_KEY=
TIMEZONE=America/New_York

# Shared HTTP Basic Auth gate for the whole site (cron endpoint excluded). When
# either is unset the gate is disabled, so the site stays open.
BASIC_AUTH_USER=66media
BASIC_AUTH_PASSWORD=

# Optional persistence (Supabase). When unset, the app still runs: it collects
# live and emails, but without cross-day dedup, once-per-day idempotency, or
# stored history. Set both to enable. Use the service-role key (server-only).
SUPABASE_URL=
SUPABASE_SERVICE_ROLE_KEY=

# Optional social monitoring (Apify pay-per-result X + Facebook keyword search).
# OFF unless SOCIAL_ENABLED is exactly "true" AND APIFY_TOKEN is set, so it stays
# dark until you've added a token and set a spend ceiling in the Apify console
# ($5/mo recommended). X (Twitter) is the backbone; Facebook is best-effort.
# Runs only on the daily digest, capped at 50 items/run. Override the actor IDs
# only if you swap in a different scraper.
SOCIAL_ENABLED=false
APIFY_TOKEN=
# APIFY_X_ACTOR=apidojo/tweet-scraper
# APIFY_FB_ACTOR=apify/facebook-search-scraper

# Optional AI relevance pass: borderline (uncertain/likely) items get a second
# opinion from Claude Haiku — confirm, downgrade, or drop. Skipped when unset;
# never blocks a run. Cost is pennies per month at digest volume.
ANTHROPIC_API_KEY=

# Official X (Twitter) API v2 Recent Search — pay-per-use ($0.005/post read,
# 24h dedup, prepaid credits with a spend cap set in the X Developer Console).
# When set, runs on every collection (poller included) and the Apify X scraper
# is skipped. Get a Bearer Token at developer.x.com.
X_BEARER_TOKEN=

# Bluesky keyword search (free). Create an account, then Settings -> App
# Passwords -> generate one (never the real password). Runs on every
# collection when both are set.
BLUESKY_IDENTIFIER=
BLUESKY_APP_PASSWORD=

# Facebook page watchlist via apify/facebook-posts-scraper (~$2 per 1,000
# posts — requires a paid Apify plan, Starter $29/mo). Scrapes recent posts
# FROM the curated public pages in src/lib/social.ts (override with FB_PAGES,
# comma-separated page URLs). Off unless FB_WATCHLIST is exactly "true".
FB_WATCHLIST=false
# FB_PAGES=https://www.facebook.com/wtopnews,https://www.facebook.com/VaDOT

# Optional near-real-time push alerts (the /api/cron/poll route, driven by the
# GitHub Actions schedule in .github/workflows/poll.yml). With both set, new
# high-signal items push to your phone via Pushover; when unset the poller
# falls back to a Resend email alert. Create an application at
# pushover.net/apps/build for the app token.
PUSHOVER_APP_TOKEN=
PUSHOVER_USER_KEY=

# LinkedIn runs in cookie-free "company-page mode": it scrapes the recent posts
# of the pages listed here (comma-separated slugs or full company URLs). Leave
# LINKEDIN_PAGES empty to skip LinkedIn entirely. This catches the pages' OWN
# posts, not public mentions (LinkedIn has no open keyword search without login).
# LINKEDIN_PAGES=66-express,virginia-department-of-transportation
# APIFY_LINKEDIN_ACTOR=apimaestro/linkedin-company-posts
